{
  "name": "wise-debug-tool",
  "private": true,
  "version": "1.0.0",
  "description": "Browser extension for debugging",
  "workspaces": [
    "ui"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "build:ui": "npm run -w ui build",
    "build": "npm run build:ui && webpack --mode production",
    "build:chrome": "npm run build:ui && webpack --mode production --env platform=chrome && zip -r dist/chrome.zip dist/chrome",
    "build:firefox": "npm run build:ui && webpack --mode production --env platform=firefox && zip -r dist/firefox.zip dist/firefox",
    "build:all": "npm run build:chrome && npm run build:firefox",
    "setup": "npm install && npm run build:all",
    "lint": "prettier --write \"**/*.{js,ts,vue,md,json}\""
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "prettier": "^3.6.2",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "engines": {
    "npm": ">=7"
  }
}
